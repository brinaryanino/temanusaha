import Link from "next/link"; import { login } from "@/server/actions/auth"; import { Card } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Button } from "@/components/ui/button";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const p=await searchParams;return <Card className="p-6 sm:p-8"><h1 className="text-2xl font-bold">Selamat datang kembali</h1><p className="mt-1 text-sm text-muted-foreground">Masuk untuk melanjutkan mengelola usaha.</p>{p.error&&<p role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{p.error}</p>}{p.message&&<p className="mt-4 rounded-lg bg-green-50 p-3 text-sm text-green-700">{p.message}</p>}<form action={login} className="mt-6 space-y-4"><label className="block text-sm font-medium">Email<Input name="email" type="email" autoComplete="email" required className="mt-1"/></label><label className="block text-sm font-medium">Kata sandi<Input name="password" type="password" autoComplete="current-password" required minLength={8} className="mt-1"/></label><div className="text-right"><Link className="text-sm font-semibold text-primary hover:underline" href="/forgot-password">Lupa kata sandi?</Link></div><Button className="w-full">Masuk</Button></form><p className="mt-6 text-center text-sm text-muted-foreground">Belum punya akun? <Link className="font-semibold text-primary hover:underline" href="/register">Daftar gratis</Link></p></Card>}
