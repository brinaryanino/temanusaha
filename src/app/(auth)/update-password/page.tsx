import { updatePassword } from "@/server/actions/auth";import { Card } from "@/components/ui/card";import { Input } from "@/components/ui/input";import { Button } from "@/components/ui/button";
export default async function UpdatePassword({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <Card className="p-6 sm:p-8"><h1 className="text-2xl font-bold">Buat kata sandi baru</h1><p className="mt-1 text-sm text-muted-foreground">Gunakan minimal 8 karakter yang sulit ditebak.</p>{p.error&&<p role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{p.error}</p>}<form action={updatePassword} className="mt-6 space-y-4"><label className="block text-sm font-medium">Kata sandi baru<Input name="password" type="password" autoComplete="new-password" required minLength={8} className="mt-1"/></label><Button className="w-full">Perbarui kata sandi</Button></form></Card>}
