import Link from "next/link"; import { register } from "@/server/actions/auth"; import { Card } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Button } from "@/components/ui/button";
export default async function Register({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <Card className="p-6 sm:p-8"><h1 className="text-2xl font-bold">Mulai kelola pelanggan</h1><p className="mt-1 text-sm text-muted-foreground">Buat ruang kerja usaha Anda dalam beberapa langkah.</p>{p.error&&<p role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{p.error}</p>}<form action={register} className="mt-6 space-y-4"><label className="block text-sm font-medium">Nama lengkap<Input name="name" required minLength={2} className="mt-1"/></label><label className="block text-sm font-medium">Nama usaha<Input name="businessName" required minLength={2} className="mt-1"/></label><label className="block text-sm font-medium">Email<Input name="email" type="email" autoComplete="email" required className="mt-1"/></label><label className="block text-sm font-medium">Kata sandi<Input name="password" type="password" autoComplete="new-password" required minLength={8} className="mt-1"/><span className="mt-1 block text-xs text-muted-foreground">Minimal 8 karakter.</span></label><Button className="w-full">Buat akun</Button></form><p className="mt-6 text-center text-sm text-muted-foreground">Sudah punya akun? <Link className="font-semibold text-primary hover:underline" href="/login">Masuk</Link></p></Card>}
