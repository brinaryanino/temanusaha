import Link from "next/link";import { requestPasswordReset } from "@/server/actions/auth";import { Card } from "@/components/ui/card";import { Input } from "@/components/ui/input";import { Button } from "@/components/ui/button";
export default async function ForgotPassword({searchParams}:{searchParams:Promise<{error?:string;success?:string}>}){const p=await searchParams;return <Card className="p-6 sm:p-8"><h1 className="text-2xl font-bold">Pulihkan kata sandi</h1><p className="mt-1 text-sm text-muted-foreground">Kami akan mengirim tautan aman ke email akun Anda.</p>{(p.error||p.success)&&<p role="status" className={`mt-4 rounded-lg p-3 text-sm ${p.error?"bg-red-50 text-red-700":"bg-green-50 text-green-700"}`}>{p.error||p.success}</p>}<form action={requestPasswordReset} className="mt-6 space-y-4"><label className="block text-sm font-medium">Email<Input name="email" type="email" autoComplete="email" required className="mt-1"/></label><Button className="w-full">Kirim tautan pemulihan</Button></form><Link href="/login" className="mt-5 block text-center text-sm font-semibold text-primary hover:underline">Kembali ke halaman masuk</Link></Card>}
