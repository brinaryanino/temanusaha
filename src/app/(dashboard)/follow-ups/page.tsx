import { db } from "@/db/client";import { requireContext } from "@/lib/auth";import { PageHeader } from "@/components/page-header";import { Card } from "@/components/ui/card";import { Input } from "@/components/ui/input";import { Button } from "@/components/ui/button";import { createFollowUp,setFollowUpStatus } from "@/server/actions/crm";import { formatDate } from "@/lib/utils";import { whatsappUrl } from "@/lib/whatsapp";import { MessageCircle,Check,SkipForward } from "lucide-react";
export default async function FollowUps({searchParams}:{searchParams:Promise<{filter?:string;error?:string;success?:string}>}){const ctx=await requireContext(),p=await searchParams,now=new Date(),start=new Date(now);start.setHours(0,0,0,0);const end=new Date(start);end.setDate(end.getDate()+1);const filter=p.filter||"overdue";const range=filter==="overdue"?{status:"PENDING" as const,dueAt:{lt:start}}:filter==="today"?{status:"PENDING" as const,dueAt:{gte:start,lt:end}}:filter==="upcoming"?{status:"PENDING" as const,dueAt:{gte:end}}:{status:"COMPLETED" as const};const [rows,customers]=await Promise.all([db.followUp.findMany({where:{workspaceId:ctx.workspaceId,...range},include:{customer:true},orderBy:{dueAt:"asc"},take:50}),db.customer.findMany({where:{workspaceId:ctx.workspaceId,deletedAt:null},select:{id:true,name:true},orderBy:{name:"asc"}})]);return <><PageHeader title="Follow-up" description="Jaga hubungan pelanggan tetap hangat dan terarah."/>{(p.error||p.success)&&<p className={`mb-4 rounded-lg p-3 ${p.error?"bg-red-50 text-red-700":"bg-green-50 text-green-700"}`}>{p.error||p.success}</p>}<div className="mb-5 flex gap-2 overflow-x-auto">{[["overdue","Terlambat"],["today","Hari ini"],["upcoming","Mendatang"],["completed","Selesai"]].map(([v,l])=><a key={v} href={`?filter=${v}`} className={`shrink-0 rounded-lg px-4 py-2 text-sm font-semibold ${filter===v?"bg-primary text-white":"border bg-white"}`}>{l}</a>)}</div><div className="grid gap-5 xl:grid-cols-[1fr_360px]"><div className="space-y-3">{rows.map(f=><Card key={f.id} className="p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><strong>{f.customer.name}</strong><p className="text-sm text-muted-foreground">{f.type} • {formatDate(f.dueAt)}</p>{f.notes&&<p className="mt-2 text-sm">{f.notes}</p>}</div>{f.status==="PENDING"&&<div className="flex gap-2">{f.customer.phone&&whatsappUrl(f.customer.phone)&&<a aria-label={`Buka WhatsApp ${f.customer.name}`} target="_blank" rel="noreferrer" href={whatsappUrl(f.customer.phone)!} className="grid min-h-11 min-w-11 place-items-center rounded-lg border text-success"><MessageCircle size={19}/></a>}<form action={setFollowUpStatus.bind(null,f.id,"COMPLETED")}><button aria-label="Tandai selesai" className="grid min-h-11 min-w-11 place-items-center rounded-lg bg-success text-white"><Check size={19}/></button></form><form action={setFollowUpStatus.bind(null,f.id,"SKIPPED")}><button aria-label="Lewati" className="grid min-h-11 min-w-11 place-items-center rounded-lg border"><SkipForward size={19}/></button></form></div>}</div></Card>)}{!rows.length&&<Card className="p-10 text-center text-muted-foreground">Tidak ada follow-up pada kategori ini.</Card>}</div><Card className="h-fit p-5"><h2 className="text-lg font-semibold">Buat follow-up</h2><form action={createFollowUp} className="mt-4 space-y-4"><label className="block text-sm font-medium">Pelanggan<select name="customerId" required className="mt-1 min-h-11 w-full rounded-lg border bg-white px-3"><option value="">Pilih pelanggan</option>{customers.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="block text-sm font-medium">Jenis<Input name="type" required placeholder="Cek kepuasan, penawaran..." className="mt-1"/></label><label className="block text-sm font-medium">Jadwal<Input name="dueAt" type="datetime-local" required className="mt-1"/></label><label className="block text-sm font-medium">Catatan<textarea name="notes" className="mt-1 min-h-20 w-full rounded-lg border p-3"/></label><Button className="w-full">Simpan follow-up</Button></form></Card></div></>}
