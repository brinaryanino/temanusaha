import Link from "next/link";import { db } from "@/db/client";import { requireContext } from "@/lib/auth";import { PageHeader } from "@/components/page-header";import { Card } from "@/components/ui/card";import { Badge } from "@/components/ui/badge";import { formatDate,formatRupiah } from "@/lib/utils";import { Plus } from "lucide-react";
export default async function Transactions({searchParams}:{searchParams:Promise<{status?:string;page?:string}>}){const ctx=await requireContext(),p=await searchParams,page=Math.max(1,Number(p.page)||1),take=10,where={workspaceId:ctx.workspaceId,...(p.status?{status:p.status as "PAID"|"DRAFT"|"CANCELLED"}:{})};const [rows,total]=await Promise.all([db.transaction.findMany({where,include:{customer:true},orderBy:{transactedAt:"desc"},skip:(page-1)*take,take}),db.transaction.count({where})]);return <><PageHeader title="Transaksi" description={`${total} transaksi tercatat.`} action={<Link href="/transactions/new" className="inline-flex min-h-11 items-center gap-2 rounded-lg bg-primary px-4 text-sm font-semibold text-white"><Plus size={18}/>Catat transaksi</Link>}/><Card className="mb-4 p-4"><form><select name="status" defaultValue={p.status||""} onChange={undefined} className="min-h-11 rounded-lg border bg-white px-3"><option value="">Semua status</option><option value="PAID">Lunas</option><option value="DRAFT">Draft</option><option value="CANCELLED">Dibatalkan</option></select><button className="ml-2 min-h-11 rounded-lg border px-4">Terapkan</button></form></Card><div className="space-y-3">{rows.map(t=><Link key={t.id} href={`/transactions/${t.id}`}><Card className="mb-3 flex flex-col justify-between gap-3 p-4 sm:flex-row sm:items-center"><div><strong>{t.invoiceNumber}</strong><p className="text-sm text-muted-foreground">{t.customer.name} • {formatDate(t.transactedAt)}</p></div><div className="flex items-center gap-3"><Badge tone={t.status==="PAID"?"success":t.status==="DRAFT"?"warning":"danger"}>{t.status==="PAID"?"Lunas":t.status==="DRAFT"?"Draft":"Dibatalkan"}</Badge><strong>{formatRupiah(t.total)}</strong></div></Card></Link>)}{!rows.length&&<Card className="p-12 text-center text-muted-foreground">Belum ada transaksi pada filter ini.</Card>}</div></>}
