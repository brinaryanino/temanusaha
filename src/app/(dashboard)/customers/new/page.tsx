import { PageHeader } from "@/components/page-header";import { Card } from "@/components/ui/card";import { CustomerForm } from "@/components/forms/customer-form";import { createCustomer } from "@/server/actions/crm";
export default async function NewCustomer({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <><PageHeader title="Tambah pelanggan" description="Simpan informasi penting agar mudah dihubungi kembali."/>{p.error&&<p role="alert" className="mb-4 rounded-lg bg-red-50 p-3 text-red-700">{p.error}</p>}<Card className="max-w-3xl p-5 sm:p-6"><CustomerForm action={createCustomer}/></Card></>}
