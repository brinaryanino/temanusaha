import { Input } from "@/components/ui/input";import { Button } from "@/components/ui/button";
export function CustomerForm({action,customer}:{action:(fd:FormData)=>void|Promise<void>;customer?:{name:string;phone:string|null;email:string|null;address:string|null;source:string|null;status:string;notes:string|null}}){return <form action={action} className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium sm:col-span-2">Nama pelanggan *<Input name="name" defaultValue={customer?.name} required minLength={2} className="mt-1"/></label><label className="text-sm font-medium">Nomor WhatsApp<Input name="phone" type="tel" defaultValue={customer?.phone||""} className="mt-1"/><span className="mt-1 block text-xs text-muted-foreground">Contoh: 0812 3456 7890</span></label><label className="text-sm font-medium">Email<Input name="email" type="email" defaultValue={customer?.email||""} className="mt-1"/></label><label className="text-sm font-medium">Sumber<Input name="source" defaultValue={customer?.source||""} placeholder="Instagram, rekomendasi..." className="mt-1"/></label><label className="text-sm font-medium">Status<select name="status" defaultValue={customer?.status||"ACTIVE"} className="mt-1 min-h-11 w-full rounded-lg border border-border bg-white px-3"><option value="ACTIVE">Aktif</option><option value="PROSPECT">Prospek</option><option value="INACTIVE">Tidak aktif</option></select></label><label className="text-sm font-medium sm:col-span-2">Alamat<textarea name="address" defaultValue={customer?.address||""} className="mt-1 min-h-24 w-full rounded-lg border border-border p-3"/></label><label className="text-sm font-medium sm:col-span-2">Catatan<textarea name="notes" defaultValue={customer?.notes||""} className="mt-1 min-h-28 w-full rounded-lg border border-border p-3"/></label><div className="sm:col-span-2"><Button>Simpan pelanggan</Button></div></form>}
